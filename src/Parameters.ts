import { FloatColor } from "./Color.js";
export type Parameters = {
  brushSize: number;
  stepSize: number;
  opacity: number;
  color: FloatColor;
  movementMin: number;
  blur: number;
  debug?: boolean;
};
